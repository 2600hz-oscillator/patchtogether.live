
## 2026-08-24 — seq-deletion PR: the half-conversion sweep (fleet log)

**The deletion's biggest hidden cost was not the 52 deleted files — it was ~35
e2e files where a bulk rename had turned `type: 'sequencer'` into `type: 'kria'`
and stopped.** Kria is not a drop-in: its run param is `running` (not
`isPlaying`), its outs are `gate1/pitch1..4` (not `gate/pitch`), it has NO
`clock` output, and — the big one — **an unseeded kria has no active pattern and
its transport is a NO-OP** (`advanceBaseTick` early-returns; verified in
kria.ts). A half-converted spec therefore either fails 0.0000 (honest) or, worse,
**passes vacuously** (the cartesian coverage row passed with a nonexistent source
port because unpatched-clock mode emits anyway — the test's subject silently
vanished).

Fixes now in `_helpers.ts`, use these and nothing else:
- `seedKriaGate(page, id, opts?)` — melodic default pattern, gate train.
- `buildKriaData(steps, opts?)` / `buildKriaMidiData(midis, opts?)` — plain-data
  builders (chromatic scale, root 48, so ANY midi is exact); pass through
  `seedKriaWith(page, id, data)` or as spawn `data:`.
- Poly chords: kria-clocked CARTESIAN (`gate1 → clock`, cells `{on,midi,chord}`)
  — but cartesian chords gate AT MOST 4 lanes (`chordVoicing` = root/3rd/5th/oct,
  poly.ts). **A 5-voice consumer needs CLIP PLAYER** (5 stacked notes on one
  lane's poly cable + TIMELORDE transport) — that is how pentemelodica's emit
  row got voice5 back.

**Rate trap, measured both ways:** several old spec comments claim the deleted
SEQUENCER stepped quarters ("240 BPM = 4 Hz"). The CODE says 16ths
(`60/bpm/4`, sequencer.ts at origin/main) — same grid as kria — so the faithful
conversion is SAME bpm, timeDivision 1. I first believed the comments and built
a ×4 slowdown; reverted. Trust the deleted module's code, never its consumers'
prose.

**Product bug found by the conversion and fixed in kria.ts:** kria's free-run
loop had NO #229 past-due drop guard (score.ts has it; kria never got it) — a
stall > lookahead bunch-fired the whole backlog in the past.
Ported: grid marches on, late emits dropped, `lateStepsDropped`/`pastDueEmits`
exposed via engine.read; tempo-stability.spec re-subjected to kria asserts it
(totalAdvances counts PER TRACK — scale by KRIA_TRACKS, imported).

Both per-port sweeps' harness kria drivers (ctx-gate-seq / driver-seq /
`<prefix>-seq` gate source) are seeded by `populateAllSequencerSteps` (behavioral)
and an inline `buildKriaData` seed (outputs) — an unseeded harness kria was
silently starving every context-gated row.

### WAVE 6 SPEC BATCH — derived 2026-08-24, ready to execute

**Remaining unspec'd bespoke, after removing the untouchables and the settled:**
`doom` + blood (NEVER touch), the five #2183 deletes (sequencer/drumseqz/macseq/
polyseqz/writeseq), and `clipplayer` (owner chose option (a): the card stays,
no face). That leaves **25**:

archivist · audioIn · cartesian · clockedRunner · controlSurface · es9 ·
gamepad · gibribbon · launchpadControlLeft · livecode · mappy · midiLane ·
moog956 · outToLaunch · painter · peertube · push2Control · recorderbox ·
textmarquee · toybox · tvLibrarian · videobox · videovarispeed · vstFx ·
vstInstrument

**Four cohorts, each sharing a DESIGN PROBLEM (not a folder):**

- **A · MEDIA / STREAM SURFACES** — archivist, peertube, tvLibrarian, videobox,
  videovarispeed, recorderbox. A remote or streamed picture IS the interaction;
  transport + source selection; the SCREEN ON/OFF ruling applies to all six;
  and `needs-media-controller` (#1511) has **12 modules waiting on it**, so this
  cohort's job is to define that capability precisely enough to schedule.
  ⚠ `videovarispeed` is the known-hard one (un-faced, `overflow: hidden`, and
  it keeps the `varispeed-panel-layout` row picturebox retired for itself).
- **B · HARDWARE CONTROL SURFACES** — controlSurface, gamepad,
  launchpadControlLeft, push2Control, outToLaunch, es9, midiLane. A physical
  device is the interaction; the roster lives behind a browser API and is READ
  from the engine — **use `getActiveEngine()`; the `env`-for-selectors ask is a
  DISPROVEN blocker** (wave 5 `BINDERS.md` §9). Mapping/assignment is the
  primary surface, not a knob list.
- **C · CANVAS / AUTHORING** — painter, toybox, gibribbon, textmarquee, mappy,
  livecode. The user edits a canvas or a text/code buffer directly. ⚠ The
  in-canvas-vs-chrome text ruling from `GAMES.md` applies verbatim here.
- **D · AUDIO / UTILITY REMAINDER** — cartesian, clockedRunner, moog956,
  vstFx, vstInstrument. (`cartesian` is now load-bearing across the e2e suite
  as the surviving note-entry + S&H host — spec it accordingly.)

**RECOMMENDED WAVE 6 = COHORT A**, fanned 3 ways (2 modules each). Highest
coherence, and it is the one whose output unblocks a named capability rather
than only producing faces.

**Every wave-6 spec must list ALL THREE gates a face PR satisfies** — wave 4's
§14 listed only two and midiclock discovered `EXTENSION_BODY_ROLES`
(`face-rack-status-source.test.ts`) the hard way.

### SKIFREE shipping-shell bug — VERIFIED, and the fix pattern ALREADY EXISTS

Claim (from wave-5 spec work) **confirmed by reading main**, not taken on trust:

- `packages/web/src/lib/audio/modules/skifree.ts:97` states it outright — *"The
  card owns the canvas + the loaded bundle's controller; the factory …"*.
- `SkifreeCard.svelte` `onMount` injects `<script src="/skifree/skifree.bundle.js">`
  and creates the controller; `onDestroy` disposes it and removes the script.

**So: no card mounted ⇒ no game.** Under the shipping shell an un-migrated
module renders as a PLACEHOLDER, so a rack containing SKIFREE has no game until
the dock pane is expanded — and collapsing the pane unmounts the card, which
disposes the controller and **destroys the run in progress**.

⚠ **This is the known `card-unmount-kills-node-resources` class (#1531 / #1574 /
#1583), and the repo already ships its fix**:
`packages/web/src/lib/ui/media/node-extras-registry.ts` — a NODE-keyed registry
that owns the resource for the node's lifetime, swept when the node leaves the
graph, rather than for the card's lifetime. `extras-producer-lifetime.spec.ts`
is the behavioural proof (its comment names the design: *a registry, NOT a
permanent off-screen card mount*), and `card-producer-lifetime.spec.ts` is the
sibling. **Whoever takes this should port skifree onto that registry, not invent
a mechanism.**

Why nothing caught it (both gates structurally blind, per the wave-5 report):
`CARD_PRODUCER_LANE_TYPES` greps `.svelte` subtrees and skifree publishes
through a `.ts`; `card-media-lifetime`'s `EXTRAS_OWNERS` never asks because
skifree is not on that channel. And **every skifree e2e boots `?shell=legacy`**,
so nothing has ever exercised the shipping path — the same legacy-shell
blindness that would have let a dead matrixMix face ship green.

Scope: ordinary bug PR, no face involved. Owner decision NOT required to fix the
lifetime bug; the separate `skifree` question in the decision queue (fork the
vendored bundle for a determinism hook, to get a VRT baseline) is independent
and should not block this.
