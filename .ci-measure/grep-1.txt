(?: depolarizer card matches baseline| drummergirl card matches baseline| drumseqz card matches baseline| moog904c card matches baseline| moog914 card matches baseline| moog961 card matches baseline| moog992 card matches baseline| moog994 card matches baseline| polyseqz card matches baseline| qbrt card matches baseline| vca card matches baseline| face audio freeze negative control: a sounding face is unstable RUNNING, identical FROZEN| face-adsr-compact: the compact lane tile matches baseline| face-analogVco-compact: the compact lane tile matches baseline| face-clouds-compact: the compact lane tile matches baseline| face-clouds-dock: the dock full-view faceplate matches baseline| face-karplus-dock: the dock full-view faceplate matches baseline| face-mixer-dock: the dock full-view faceplate matches baseline| face-noise-compact: the compact lane tile matches baseline| face-pentemelodica-compact: the compact lane tile matches baseline| face-pentemelodica-dock: the dock full-view faceplate matches baseline| face-qbrt-compact: the compact lane tile matches baseline| face-qbrt-dock: the dock full-view faceplate matches baseline| face-ringback-compact: the compact lane tile matches baseline| face-snaredrum-dock: the dock full-view faceplate matches baseline| face-tomtom-dock: the dock full-view faceplate matches baseline| face-vca-compact: the compact lane tile matches baseline)$
