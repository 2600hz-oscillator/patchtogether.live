(?: dx7 card matches baseline| filter card matches baseline| moog903a card matches baseline| moog905 card matches baseline| moog921a card matches baseline| moog960 card matches baseline| moog962 card matches baseline| moog995 card matches baseline| reverb card matches baseline| scaler card matches baseline| score card matches baseline| wavecel card matches baseline| dock fold negative control: the capture sees below the old fold, and the old one did not| face-analogVco-dock: the dock full-view faceplate matches baseline| face-bluebox-dock: the dock full-view faceplate matches baseline| face-clap-compact: the compact lane tile matches baseline| face-cofefve-compact: the compact lane tile matches baseline| face-delay-dock: the dock full-view faceplate matches baseline| face-drummergirl-compact: the compact lane tile matches baseline| face-dx7-dock: the dock full-view faceplate matches baseline| face-filter-dock: the dock full-view faceplate matches baseline| face-karplus-compact: the compact lane tile matches baseline| face-kickdrum-compact: the compact lane tile matches baseline| face-lfo-compact: the compact lane tile matches baseline| face-macrooscillator-dock: the dock full-view faceplate matches baseline| face-rings-compact: the compact lane tile matches baseline| face-shimmershine-compact: the compact lane tile matches baseline| face-tidyVco-dock: the dock full-view faceplate matches baseline| face-vca-dock: the dock full-view faceplate matches baseline)$
