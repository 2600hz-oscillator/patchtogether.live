(?: buggles card matches baseline| cartesian card matches baseline| charlottesEchos card matches baseline| illogic card matches baseline| mixer card matches baseline| moog904b card matches baseline| moog907a card matches baseline| moog923 card matches baseline| moog984 card matches baseline| noise card matches baseline| sequencer card matches baseline| stereovca card matches baseline| sticky card matches baseline| face-bluebox-compact: the compact lane tile matches baseline| face-cloudseed-compact: the compact lane tile matches baseline| face-cofefve-dock: the dock full-view faceplate matches baseline| face-cube-dock: the dock full-view faceplate matches baseline| face-delay-compact: the compact lane tile matches baseline| face-drummergirl-dock: the dock full-view faceplate matches baseline| face-dx7-compact: the compact lane tile matches baseline| face-kickdrum-dock: the dock full-view faceplate matches baseline| face-mixer-compact: the compact lane tile matches baseline| face-resofilter-compact: the compact lane tile matches baseline| face-resofilter-dock: the dock full-view faceplate matches baseline| face-reverb-compact: the compact lane tile matches baseline| face-ringback-dock: the dock full-view faceplate matches baseline| face-rings-dock: the dock full-view faceplate matches baseline| face-shimmershine-dock: the dock full-view faceplate matches baseline| face-sixstrum-dock: the dock full-view faceplate matches baseline| face-tomtom-compact: the compact lane tile matches baseline)$
