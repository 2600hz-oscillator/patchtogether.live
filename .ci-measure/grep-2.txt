(?: adsr card matches baseline| destroy card matches baseline| meowbox card matches baseline| mixmstrs card matches baseline| moog911a card matches baseline| moog912 card matches baseline| moog921b card matches baseline| moog956 card matches baseline| moog993 card matches baseline| polarizer card matches baseline| shimmershine card matches baseline| wavetableVco card matches baseline| every shipped face has a scene, and every scene has its baselines| face-adsr-dock: the dock full-view faceplate matches baseline| face-clap-dock: the dock full-view faceplate matches baseline| face-cloudseed-dock: the dock full-view faceplate matches baseline| face-cube-compact: the compact lane tile matches baseline| face-filter-compact: the compact lane tile matches baseline| face-lfo-dock: the dock full-view faceplate matches baseline| face-macrooscillator-compact: the compact lane tile matches baseline| face-marbles-compact: the compact lane tile matches baseline| face-marbles-dock: the dock full-view faceplate matches baseline| face-meowbox-compact: the compact lane tile matches baseline| face-meowbox-dock: the dock full-view faceplate matches baseline| face-noise-dock: the dock full-view faceplate matches baseline| face-reverb-dock: the dock full-view faceplate matches baseline| face-sixstrum-compact: the compact lane tile matches baseline| face-snaredrum-compact: the compact lane tile matches baseline| face-tidyVco-compact: the compact lane tile matches baseline)$
