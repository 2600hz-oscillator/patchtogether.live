.svelte-kit/
build/
.vite/
.env
.env.*
!.env.example

# DOOM module — generated by native/build-doom-wasm.sh (not in git: WASM
# is deterministic enough that contributors + CI build locally). DOOM1.WAD
# is the shareware game data — see static/doom/DOWNLOAD_INSTRUCTIONS.md.
static/doom/doom.js
static/doom/doom.wasm
static/doom/doom.data
static/doom/DOOM1.WAD
# Slice-1 MP Node test artifact (built on demand by the net_pt acceptance
# harness; same gitignore rationale as the prod build above).
static/doom/doom-mp-node.js
static/doom/doom-mp-node.wasm
# DOOM_MP=1 web build artefact (built on demand; same rationale).
static/doom/doom-mp.js
static/doom/doom-mp.wasm
native/doomgeneric/build/

# SNES9X module — generated by native/build-snes9x-wasm.sh (not in git:
# WASM is deterministic, contributors + CI build locally from the vendored
# snes9x2005 core under native/snes9x/). The ROM is user-provided + gitignored
# below (see static/roms/snes9x/README.md + `task setup:snes9x`).
static/snes9x/snes9x.js
static/snes9x/snes9x.wasm
