<script lang="ts">
  // LAUNCHPAD CONTROL · RIGHT — module docs. Right-click the card → "View docs"
  // opens this page (/docs/modules/launchpadControlRight). Renders the same
  // SHARED Launchpad guide as the LEFT page (one colour-coded source of truth).
  import LaunchpadDocs from '$lib/docs/LaunchpadDocs.svelte';
</script>

<svelte:head>
  <title>Launchpad control (right / deck + editor) · modules · patchtogether.live</title>
  <meta
    name="description"
    content="The RIGHT Novation Launchpad Mini Mk3 is the command deck (EDIT / COPY / PASTE / DOUBLE / LENGTH / NOW + per-lane STOP + transport) and flips to the 8-pitch × 8-step note editor while you edit. Pairing, pad/CC map, SHIFT windowing, and the full LED colour language."
  />
</svelte:head>

<LaunchpadDocs />
