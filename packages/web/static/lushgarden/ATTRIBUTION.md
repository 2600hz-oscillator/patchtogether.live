# LUSH GARDEN atlas — PLACEHOLDER SET

These 8 images are clearly-fake, procedurally generated plant
cutouts (CC0), committed ONLY so the lushgarden module is testable while the
real 140–170-cutout atlas is produced by the parallel asset workflow. When
the real atlas lands, this whole directory is replaced (manifest.json +
PNGs + this file).

Generator: scratchpad gen-lushgarden-placeholders.mjs.
