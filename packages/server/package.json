{
  "name": "@inet.modular/server",
  "version": "0.0.0",
  "private": true,
  "description": "Hocuspocus + (later) WebRTC signaling for inet.modular multi-user rackspaces. Stage B onward.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hocuspocus/server": "^2.13.5",
    "yjs": "^13.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
